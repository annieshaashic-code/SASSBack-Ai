# Copy this file to .env and fill in your values

# OpenAI API Key (optional – app works with fallback comebacks without it)
# Get one free at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# Flask environment (development | production)
FLASK_ENV=development

# Port (default 5000)
PORT=5000

# SQLite database file path (default: fairy_godmother.db in backend folder)
DB_PATH=fairy_godmother.db
